{% extends 'base.html.twig' %}

{% block title %}Créer un plat{% endblock %}

{% block body %}
    <div class="row">
        <div class="offset-3 col-6">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <hr>
            <button class="btn btn-success">Ajouter le plat</button>

            {{ form_end(form) }}
        </div>

    </div>
{% endblock %}
